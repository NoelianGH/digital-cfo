/**
 * Service untuk memproses Speech-to-Text.
 * Nantinya file ini akan diisi logika untuk mengubah input suara pengguna menjadi teks transaksi.
 */

export async function transcribeAudio(audioBlob: Blob) {
  // TODO: Implementasi integrasi Speech-to-Text (misal: Web Speech API, OpenAI Whisper, dll)
  return {
    success: false,
    text: ""
  };
}
