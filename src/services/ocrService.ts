/**
 * Service untuk memproses Optical Character Recognition (OCR).
 * Nantinya file ini akan diisi logika untuk membaca teks dari foto/gambar struk atau buku kas fisik.
 */

export async function processOcrScan(imageFile: File) {
  // TODO: Implementasi integrasi OCR (misal: Tesseract, Google Vision, dll)
  return {
    success: false,
    extractedData: null,
    rawText: ""
  };
}
